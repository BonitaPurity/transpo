databases:
  - name: transpo-db
    plan: free

services:
  - type: web
    name: transpo-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: transpo-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
